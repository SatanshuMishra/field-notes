# Field Notes — Project Ledger

## Goal
A personal journaling app for macOS + Android with a cozy, hand-drawn cel-shaded aesthetic (prototype "Field Notes"). Daily entries as voice/video/text plus photo "memories," one mood-flower per day, streaks, daily reminders. Local-first, optionally syncing end-to-end-encrypted through the user's self-hosted Arch Linux server. Open-source on GitHub (repo SatanshuMishra/field-notes, private for now).

## Constraints
- No paid Apple/Google developer account; manual sideload/install. macOS + Android only (no iOS).
- Works fully standalone with no server; sync is an optional v2 layer.
- Fast, secure (E2EE), robust; offline-capable; reachable via Tailscale.
- Binary assets must be human-provided + committed (harness blocks agent/main-thread downloads).

## Active Decisions
- decisions/2026-08-02-font-weight-variation-verdict.md — the toolchain gate that blocked the last MSP in the spec, ANSWERED AFFIRMATIVELY: on Flutter 3.44.8 `TextStyle.fontWeight` DOES drive a variable font's `wght` axis, so text-bearing goldens are safe to capture. Proven by a w100-w900 sweep rather than a two-point w300/w700 compare, because synthetic bold would be indistinguishable on two samples: each family plateaus at its OWN published fvar range (Instrument Sans 400-700, Newsreader 200-800, Caveat 400-700), which synthetic bold cannot produce. A negative control (unresolvable family falling back to Ahem) returned a flat 928.0 at all nine weights, proving the probe can report a null. Docs agree — landed 3.39.0-0.0.pre, stable in 3.41. LOAD-BEARING RESIDUE: weights outside a family's fvar range are SILENT NO-OPS (w100-w300 are identical to w400 in Instrument Sans and Caveat), and `pubspec.yaml:22`'s `sdk: ^3.12.2` is the DART constraint and does NOT pin Flutter — H1 added `environment: flutter: '>=3.41.0'` as the only machine-enforced local pin. An SDK downgrade below 3.41, or a re-exported font whose fvar range shifts, silently invalidates every text-bearing golden
- decisions/2026-08-02-goldens-pin-macos-ci.md — `.github/workflows/goldens.yml` ships on `macos-latest` pinned to the exact string `3.44.8`, running `flutter test --tags golden` with failure artifacts uploaded and a `paths:` filter. macOS is FORCED, not preferred: the default comparator is exact-pixel at zero tolerance and macOS/Linux differ in Skia rasterization and font hinting, so byte-identical PNGs across the two are unachievable. COST: macOS runners bill at a 10x multiplier, ~60-120 billable minutes per run, and the `paths:` filter is the only thing keeping it off unrelated PRs. Rejected a percentage-tolerance comparator in all variants — a tolerance loose enough to absorb cross-OS font rendering also absorbs the one-pixel shadow-offset regression the cluster exists to catch; rejected a pinned Linux capture container (a workflow tax on the one operation that must not be got wrong) and no-CI (fails H1's acceptance criterion outright). CI IS STILL NOT EVIDENCE, and the job HAS NEVER EXECUTED — the first PR touching its filter is its first run, and a mass red there must not be answered with `--update-goldens`
- decisions/2026-08-02-h1-splits-by-font-risk.md — the parent spec's single H1 decomposes into FOUR stacked MSPs ordered by font risk, not convenience: H1 harness plus the three text-free cross-hatch goldens, H2 the blooms, H3 sticker cards plus nav icons, H4 the three text-bearing sticker buttons at the TOP. The text-bearing family can then be dropped or re-captured without losing the harness beneath it, and a red in H1 is a harness failure and nothing else. Each MSP is additive and independently green, so the green-branch invariant holds at every layer and the stack merges top-down in one cascade. Shipped as stack 127 (#122 slice, #123-#126); every MSP predicted its own test total exactly — 922, 934, 941, 944, four for four. The bloom set covers TWELVE kinds, not the spec's stated ten: FlowerKind declares twelve of which wiltingRose and thistle are `ambientOnly`, and covering all twelve costs nothing | Demoted 2026-08-02 for cap, files unchanged on disk: decisions/2026-08-01-macos-visual-pass-confirmed.md (the §5.4 pass, superseded in effect by the E+F and G pass records), decisions/2026-07-29-c7-rows-wait-on-the-visual-pass.md (the pass happened; C7's surviving open items live in the thread's Open Risks), decisions/2026-07-28-force-push-permission-granted.md (spent — granted and used without incident through Clusters D-H). Summaries in sessions/2026-08-02-02-prototype-design-alignment.md
- decisions/2026-08-01-cluster-e-ships-as-a-native-github-stack.md — SUPERSEDES the `--base` is always `main` ban. Cluster E ships as ONE native GitHub stack (E1 base `main`, E2 base e1, E3 base e2, E4 base e3), unblocking all four MSPs in a single pass. The superseded ban rested on "GitHub retargets only when the base branch is DELETED", which describes the legacy 2020 retargeting behaviour, NOT the native Stacked PRs feature (public preview 2026-07-30) that tracks a stack as a first-class server object and does a server-side cascading rebase on merge. Base-chaining alone is NOT a stack — plain `gh pr create --base` yields an ordinary chained PR with no cascade; the PRs must be linked via `POST /repos/SatanshuMishra/field-notes/stacks` with `pull_requests` ordered bottom-to-top (`/stacks/{n}/add` to extend). GraphQL is read-only for stacks; the `gh stack` extension is deliberately NOT installed. Verified live: the stacks endpoint returns 200 `[]` on this repo. Merging a mid-stack PR merges every unmerged PR below it in the same operation, so merge order is structurally bottom-up. OPEN RISK (unverified in the docs): whether the cascade needs the merged head branch deleted — mitigate by deleting `msp-cluster-e/*` branches on merge and confirming each next PR's base retargeted to `main` before merging it. The Cluster E slice ships as its own docs PR outside the stack
- decisions/2026-08-01-d2-shell-destination-promoted-to-riverpod.md — D2's tap-to-Calendar is IMPLEMENTED, never dropped: dropping an acceptance criterion is a scope reduction only the user may authorize, and they authorized the opposite. `_AppShellState._selected` was a private setState field no descendant could reach; it is promoted to `@Riverpod(keepAlive: true) class ShellNavigation` in `lib/state/shell_navigation.dart`, NOT in `lib/app/shell/` — that placement shrinks the widening of the slice's closed `lib/app/shell/**` fence to EXACTLY ONE FILE (`app_shell.dart`) and avoids a features-to-app import cycle. Same declared-one-file shape as D3's optional `labelStyle` and C6's optional `headline`: additive, behaviour-preserving, every pre-existing shell test passing unmodified — and those tests transitively prove AppShell reads the provider, so the new tap receipt asserts only the cell-to-provider link and nothing is double-asserted. `keepAlive` is load-bearing, not decoration: `@riverpod` is autoDispose BY DEFAULT in Riverpod 3, and the first version silently dropped the destination whenever nothing watched it — the new test went red on exactly that and green after, making it a real receipt rather than a test written to pass. N25 honoured: `button: true`/`onTap` on the OUTER Semantics, outside ExcludeSemantics. Rejected: prop-drilling a callback through three D1-owned files to avoid touching one; `Navigator.push` of CalendarScreen, which is not the shell destination the criterion names and strands the nav rail's selected state; and dropping the criterion, which would have shipped Cluster D knowingly incomplete a second time
- decisions/2026-07-28-c5-ships-as-is.md — C5 ships knowingly incomplete on both open items. Its 6px monospace thumbnail caption stays UNSHIPPED because `EntryPhoto` has no label field and inventing one would answer OQ-6 as a side effect of a styling MSP, so OQ-6 stays OPEN and `Type.monoThumbSans` stays unconsumed. Its tilt stays on `Entry.id` parity — which matches the slice's stated mechanism exactly, but scatters pseudo-randomly because the id is a ULID, so it does NOT visibly satisfy the acceptance criterion's strict left-right alternation. That is deferred to the §5.4 macOS visual pass, which now carries two cluster-wide rulings: this and C1's flame cusp at (9,8). Strict alternation would need a feed index inside `EntryCard`, outside C5's fence
- decisions/2026-07-28-direct-implementer-waves-for-all-remaining-clusters.md — the WHOLE remaining spec (Clusters C-H) executes as direct `implementer` waves, one PR per MSP with a local `fullValidationCmd`; mitosis is not dispatched again for this spec unless its defect is fixed first. Every remaining cluster has an in-cluster dependency chain, verified against the parent spec's own `Depends on` lines (D2/D3/D4 -> D1; E2/E3 -> E1 and E4 -> E1+E2+E3; F2 -> F1, F3 -> F1+F2, F4 -> F1; G2/G5/G7 -> G1, G3 -> G2, G6 -> G5, G8 -> G6+G7), and H1 is a single MSP. So 2026-07-28-cluster-c-skips-mitosis.md's carve-out — "a cluster with no in-cluster chain is unaffected" — applies to ZERO remaining clusters and is now SPENT. No slice exists for D-H either, and C5/C6 already exist as built branches. Rejected: cutting a D slice and dispatching anyway, accepting parks and recovering afterwards. The real unlock is repairing the `saga.mjs:63` / `run-engine.mjs:100` force-with-lease collision, which would free the 21 D-H MSPs
- decisions/2026-07-28-retargeting-an-existing-test-is-not-fence-widening.md — an MSP that changes a public rendering MAY retarget the existing assertions pinning it, in test files its fence row omits; that is maintenance of an existing receipt, not a new test, so §5.2's no-new-test rule does not bar it, and a fence row omitting such a file is a spec defect rather than a reserved decision. The retarget must pin the SAME BEHAVIOUR through the changed surface, not preserve assertion count — C6's one copy string became two, so the faithful retarget asserts both. Three of four Cluster C Wave 1 MSPs hit this shape independently. HARD CARVE-OUT: `test/features/entry_cards/playback/**` and the eight files of the 106-case suite are NEVER edited (N24); a red there is fixed in the implementation or the MSP stops. Per-file and per-report — an agent finding a second out-of-fence failure stops again rather than generalizing
- decisions/2026-07-27-shared-file-cluster-serializes.md — when a cluster's MSPs share a file, that file is a HARD dependency edge declared in the slice, never left to the engine's graph to infer. All four Cluster B MSPs edit `lib/app/shell/sidebar_shell.dart`, so the chain `B1 -> B2 -> {B3, B4}` is a file-contention chain and the second of B3/B4 to reach integration MUST rebase onto the first. Costs in-cluster parallelism (B is effectively serial); accepted, because the engine catches overlapping HUNKS, not two coherent-but-incompatible rewrites of one widget. Every later slice (C-H) computes its own file-overlap matrix before dispatch
- decisions/2026-08-02-cluster-g-visually-confirmed.md — Cluster G's macOS visual pass PASSED after the user merged stack 121, so completion criterion 4 is MET for Clusters A-G and every capture surface (composer shell, note paper surface, chooser bottom sheet, voice stage, dark video viewport). It also ACCEPTS the known degradation G8 shipped: `camera_macos` implements no pause in either its Dart or Swift layer, so video pause is unreachable on macOS and shipped as a `supportsPause` capability degradation — the criterion is met on Android only and that asymmetry stands, confirmed acceptable on hardware. Patching the vendored plugin is a separate MSP, never a G8 defect; widening G8's fence into `third_party/` mid-cluster was rejected. NOT closed by a visual pass and still open: #119's undeclared `SettingsSelect` Flexible/ellipsis ride-along and #120's failed-save re-arm hole (the timer half pre-existed on `main`; the frozen readout is new). #116's desktop chooser branch stays unproven BY THE SUITE, since `flutter test` forces `defaultTargetPlatform` to ANDROID, but is now human-confirmed on hardware, which is the coverage that mattered. H1 (goldens) is the only MSP left in the spec and has no visual surface of its own
- decisions/2026-08-01-cluster-e-and-f-visually-confirmed.md — the ONE combined macOS visual pass over Cluster E's flower art and Cluster F's mood picker PASSED, so completion criterion 4 was MET for all 28 MSPs merged at `a6cdcc2` (A1-A5, B1-B4, C1-C7, D1-D4, E1-E4, F1-F4). It closed the re-opening that decisions/2026-08-01-macos-visual-pass-confirmed.md took on when Cluster E landed, and discharged the explicit not-verified line every Cluster F PR carried saying no agent can run the app. Two consequences carry forward: decisions/2026-08-01-meadow-keeps-the-shared-stroke.md ruled on the MECHANISM and left the pixels owed to this pass, and the pass reported no defect, so the shared `_stroke(d)` stands with no amendment and no second stroke path; decisions/2026-08-01-f4-cue-precedes-the-write.md stays as shipped, since the audible-cue-then-silent-failure sequence was not reported as reading wrong on hardware. STILL OPEN and never ruled on: C7's badge/chip anchors and the chip's tokenless `#2A241D`, which were never shipped so there is nothing on screen to judge. Superseded in coverage by the Cluster G record above, which extends the same criterion through Cluster G
- decisions/2026-07-27-primitives-cluster-confirms-by-regression.md — a primitives-only cluster is confirmed by REGRESSION, never by prototype match. Cluster A holds NO screen-composition MSP (Today is C1-C7, right rail D1-D4, nav rail B1-B4, flower art E1-E4, picker F1-F4, composers G1-G8), so the Today screen correctly looks unchanged after it. The flower DOMAIN MODEL was always right; FlowerPainter is what is old (stem + leaf, bloom squashed to the top ~55%; the prototype's glyphs are headless)
- decisions/2026-07-27-cluster-a-scoped-spec.md — MITOSIS HAS NO SCOPE PARAMETER. Its input parser accepts only `{spec, repoRoot, baseBranch, sourcePrefix, verify, build, models, worktreeRoot, fixLoopMax, retry}` (`mitosis.js:3303-3312`) and its decomposer reads the document at `${spec}` and decomposes ALL of it (`:3762-3768`). Scope a run by cutting an EXECUTION SLICE: a spec containing only the target cluster's MSPs verbatim, plus every constraint/finding/verification rule that binds them, plus a hard scope fence. The slice is committed and landed on base before dispatch — scratchpad placement breaks relaunch (`computeLogicalRunId` hashes the spec path `:307-308`; the manifest's `specContentHash` must match `:1496-1502`). Clusters B-H each need their own slice; the parent spec stays the authority and citation source
- decisions/2026-08-01-f4-cue-precedes-the-write.md — F4's `pencil` cue fires BEFORE the mood write, per the slice's own control-flow block and prototype `:1348`, NOT per slice resolution R10's prose, which said a failed write produces no cue. The slice contradicted itself and only the write-failure path observes it; R10's prose is the defective half. Consequence: a FAILED write plays the cue and shows no toast, and only the DIALOG is gated on an existing mood — the cue and toast fire on every successful write through either branch. N20 intact: the cue routes through `GatedSoundService` so it respects the Sound effects setting and swallows playback errors, and the inline error string on a failed write survives. Cheap to reverse (a statement reorder in one method; no test asserts cue-before-write ordering) if the audible-cue-then-silent-failure sequence reads wrong on hardware. Rejected: following R10's prose, which would have split the two branches' cue handling and contradicted the prototype the whole spec exists to match
- decisions/2026-08-01-meadow-keeps-the-shared-stroke.md — E1's change to the ONE shared `_stroke(d)` is accepted as shipped; the meadow does NOT revert to `Palette.ink`, and there is no spec amendment and no second stroke path. E1's mandate was precisely that the stroke stop hardcoding `Palette.ink`; composition is preserved literally (every garden plant kept stem, leaf and centre at `height*0.42`, byte-identical geometry) and only outline hue and weight moved (at d=44: chrysanthemum 1.5 -> 0.8, spider lily 1.5 -> 1.8). The meadow's outline colour was never a declared preserve item, so forking a shared painter to hold an undeclared value buys nothing and costs two stroke paths kept in step forever. Rejected: the amendment plus a second path in `lib/features/garden/paint/meadow_painter.dart`. Rules on the MECHANISM, not the pixels — the §5.4 macOS visual pass on the flower art is still owed and a defect it reports is a new finding, not a reopening
- decisions/2026-07-27-prototype-alignment-open-questions.md — five of the seven open questions in the prototype-alignment spec resolved, all to the recommended option. Two govern a reusable rule: adopt the prototype's FORM, never its PROMISES (its sync copy and markdown placeholder describe capabilities the app lacks, so B4 and G2 take the treatment with truthful strings). Two more subordinate design fidelity to capability: the streak card keeps its `longest` number and the video card keeps its 21:9/200px playback envelope, both of which the prototype never modelled — the card's hatch/badge/chip land as new MSP C7, its 130px height stays rejected. OQ-4 is the one app-only element deliberately DROPPED (the desktop `Capture` button); the chooser survives on phone via `app_shell.dart:30-32`, and three test files change with the removal. OQ-3 and OQ-6 remain open and touch no MSP
- decisions/2026-07-26-msp4-today-virtualization-not-authorized.md — MSP 4 / Phase 3 (Today-feed virtualization) is NOT authorized and the thread closed without it. Phase 1 (poster-first) was the root fix and retired the motivation (at-rest decoders 6 -> 0); `cacheExtent` keeps ~3-4 cards alive so virtualization never made "only visible cards decode" true; MSP 3's Option B does NOT transfer (the Today feed has no bounded ancestor, verified at main `today_screen.dart:39`/`:44`), so Phase 3 needs a genuine sliver rewrite of two structurally different trees; and `entries_dao.dart` has no `.limit(` anywhere, so pagination may beat virtualization and must not be picked silently. Both spec gates (`:128-131`) remain unmet: a post-Phase-1 hardware profile, then spec-owner review. Never execute on prediction
- decisions/2026-07-28-stacked-msps-ship-sequentially.md — MSPs stacked on a shared file ship ONE AT A TIME, never as parallel PRs. The repo squash-merges, so once the frontmost MSP lands, `main` holds its content under a SHA absent from the next branch's history — a stacked PR would then diff that content twice and risk a merge conflict. Open only the frontmost PR; when it merges, `git rebase --onto main <previous-MSP-tip>`, re-run `fullValidationCmd` on the NEW base, then open the next. Never carry a green from one base to another. Verified: after #63 and #64 merged, `git diff main 54fd9ef` was EMPTY, proving the squashes reproduced the checkpoint content exactly. Rejected: `--base <previous-branch>` stacking with GitHub auto-retarget — clean review diff, unresolved post-squash conflict
- decisions/2026-07-25-didupdatewidget-gate-ratified-as-amendment.md — Phase 1b's decode gate covers BOTH passive entry points (`initState` AND `didUpdateWidget`); only user intent acquires a slot on a poster card. Ratifies MSP 2's fifth edit (the reviewer-confirmed Today-feed resolver-swap bypass) and supersedes its interim `staysGated` form with plan Task 3's `_deferDecodeUntilIntent` (releases the held slot, closes the MEDIUM)
- decisions/2026-07-25-poster-first-supersedes-eager-decode.md — the eager-decode premise ("macOS writes no thumbnail") is FALSE: the vendored plugin's `takePicture()` reads the live sample buffer, fed during recording by an `AVCaptureVideoDataOutput` beside the movie output. Ship poster-first (thumbnail at capture, decoder only on user intent, keyed on `thumbnailMediaId != null` and NEVER on platform, so Android inherits it free); virtualization staged after. At-rest decoders 6 -> 0. The bug was MISALLOCATION (cap consumed in mount order, not viewport order), never exhaustion — so the cap stays 6 global and the per-platform 6/3 split recommended earlier the same session is rejected
- decisions/2026-07-25-video-preview-width-driven-and-cover-filled.md — the card preview sizes from its WIDTH at 21:9 with a 200px floor (no max cap — a cap reinstates the flat height the user complained about), and the live surface cover-fills via `FittedBox` over a ratio-carrying unit box. Fixes a latent poster-vs-video `BoxFit` mismatch that made the preview jump at playback start. The whole responsive band is UNRECEIPTED — the 360px-wide harness lands on the floor, so the green suite proves nothing about it
- decisions/2026-07-25-null-mutation-means-redundant-or-uncovered.md — when a mutation reds nothing, decide WHICH it is before acting: already pinned by another receipt means redundant (collapse it); not covered at all means uncovered (keep it, write the receipt). Hit both readings in one session with opposite correct responses — the voice card's `_preparedMediaId` was collapsed, the overlay's `_hidden` latch-clear got the missing receipt
- decisions/2026-07-24-overlay-auto-hide-keeps-the-youtube-model.md — controls auto-hide after 3s idle even with the pointer resting inside; the 1.4.13-clean `&& !pointerInside` alternative was priced at one term and declined by the user. The spec's w3c/wcag#2007 citation is wrong (that issue is SC 2.4.7, not 1.4.13). Compliance rests on three mandatory mitigations: focus-within suspends auto-hide, `ExcludeFocus` is forbidden so hidden controls stay Tab-reachable, and `accessibleNavigation` disables the timeout outright
- decisions/2026-07-24-denied-claim-uses-a-live-region.md — a denied explicit slot claim raises a `Semantics(liveRegion: true)` notice plus a transport hint, transport left enabled so retry stays reachable. NOT `SemanticsService.announce` (deprecated since Flutter 3.35; the SDK prefers implicit Semantics because Android deprecated announcement events). The denial bit is set only under `evictUnpinned` rights and is sticky across `_enterWaiting`
- decisions/2026-07-24-restart-retains-its-decoder-slot.md — `_restart` never releases its slot; `_attemptLoad` reuses the held token and touches it for LRU parity. Literal re-acquire-before-release is impossible (own token blocks the re-acquire, no swap API), and `evictUnpinned` would both reverse the non-evicting-acquire record and evict a third innocent holder. `_markUnavailable` releases so a re-prepare ending unavailable cannot strand a slot
- decisions/2026-07-24-playback-recovery-counts-as-interactive.md — mid-playback error recovery re-acquires WITH eviction rights; a watched card is user-driven, not passive. Supplements the non-evicting-acquire record, which review flagged as contradicting the shipped code
- decisions/2026-07-24-non-evicting-acquire-for-passive-mount.md — passive acquisition (mount, slot-freed wake-up, resolver re-prepare) acquires WITHOUT eviction rights; only user-driven acquisition may evict. Fixes a wake/evict stampede (one release made every waiting card evict a healthy holder, cascading) and the first-paint inversion in one change
- decisions/2026-07-24-video-decoder-slot-cap-and-structural-retry.md — gate `_prepare()` on a hard-capped LRU `VideoSlots` registry (cap 6, pin-while-playing), `initState` trigger unchanged; classify failures STRUCTURALLY because retryability is unreadable from the error (macOS drops `error.code`, Android passes `details: null`, and exhaustion may STALL rather than throw since `initialize()` has no timeout). Slivers rejected (macOS Today is `withRail` in a `SingleChildScrollView`); interaction gating rejected (the macOS recorder writes no thumbnail, so poster slot 1 is dead there forever); viewport gating deferred behind the hardware run
- decisions/2026-07-24-eager-init-decoder-ceiling.md — the feeds are non-lazy Columns, so eager per-card video init can exhaust the device's bounded decoder pool (~8-16 H.264), and the failure path renders the RED placeholder this branch exists to remove. Gating init (visibility or first interaction) + a retryable-vs-corrupt split is IN the video-card thread's scope and sequenced BEFORE the overlay; qualifies but does not supersede the eager-player decision
- decisions/2026-07-24-video-card-eager-player-and-controls.md — the 2 post-merge video defects are BOTH pre-existing and share one root: the card lazily latches to a bare chrome-less `VideoPlayer` and macOS capture never writes a thumbnail, which `MediaImage` renders as the RED corrupt placeholder. Fix = rebuild on the voice card's architecture (eager init, never swap the tree, poster as a fallback slot, red reserved for real failure) + hover-reveal controls. Native poster extraction and a 2nd blob migration rejected as disproportionate
- decisions/2026-07-24-blob-extension-playback-root-cause.md — extensionless SHA-256 blobs broke playback: AVFoundation picks its demuxer from the path UTI and never content-sniffs local files, so both video_player and just_audio failed together (-12847/-11828). Store the extension in rel_path + backfill + resilient read path (PR #35). Export was broken identically. Link shim rejected (dart:io has no hard-link API; links outside blobs/ defeat Delete-All and GC)
- decisions/2026-07-25-verify-squash-against-remote-tip.md — a squash merge captures a SNAPSHOT, not a branch ref; any commit pushed between snapshot and merge is lost silently. Run `git diff --stat origin/main <remote-tip>` before deleting a branch. Cost PR #38 two ledger files
- decisions/2026-07-25-msp-branch-prefix-not-per-type.md — one `msp/` prefix per mitosis run (batch identity); semantic Conventional Commit types stay on commits and squashed PR titles
- decisions/2026-07-22-black-window-standalone-binary.md — BLACK window = launching the standalone .app binary yields no first frame (null layer tree); `flutter run -d macos` renders. Always run via `flutter run`, never the raw binary. VM screenshot works only against a flutter-run instance
- decisions/2026-07-21-gh-merge-hook-blocked-human-merges.md — `gh pr merge` + the REST merge endpoint are hook-blocked for ALL callers incl. the main thread; the HUMAN merges each PR on GitHub after the agent validates locally. Supersedes the merge mechanism of 2026-07-12-human-gated-merge-policy. `git push` still works, so the agent can still resolve capture-* branch conflicts.
- decisions/2026-07-20-one-run-completion-frontier-train.md — the engine does FRONTIER-TRAIN build-ahead (code-verified): ONE human-gated run completes all 31 if PRs are merged promptly; fix the 2 parked plans first; batching was an expired debugging control, not an engine limit
- decisions/2026-07-20-keep-stale-worktrees.md — the ~24 .fireplace-worktrees checkouts are KEPT for manual testing after build/deploy; cleanup is never to be proposed again
- decisions/2026-07-20-ci-gates-are-hollow-for-dart.md — NEITHER GitHub check runs a Dart test (receipts = node-only, 15s; D6 has no Dart import grapher and passes vacuously). Run `fullValidationCmd` locally against the PR head worktree before every merge; never accept receiptsPass/d6Pass as evidence
- decisions/2026-07-20-reminders-day2-prearm-followup.md — reminders ships arming only the NEXT occurrence; day-2+ reach deferred to a batch-3 follow-up MSP (pre-arm ids 1001..1007, inside the existing fileScope)
- decisions/2026-07-20-reminders-android-desugaring-scope.md — reminders' fileScope expanded by one file (android/app/build.gradle.kts) so it ships the flutter_local_notifications desugaring config in the same PR; receipts CI runs no Android build and cannot catch this
- decisions/2026-07-16-manifest-fold-defect-and-batch-scoping.md — run.json MUST be one compact line (pretty-print breaks `foldRunManifest` -> silent full re-decompose + overwrite); engine has NO MSP-filter input, so scope batches by out-of-band trim from the pristine backup; batch 1 = 4 leaf MSPs
- decisions/2026-07-16-pre-relaunch-main-reconciliation.md — reconcile local main onto origin/main before EVERY mitosis relaunch; the engine cuts worktrees from the bare LOCAL `main` ref (mitosis.js:946/:1114), so local main must contain every merged dependency
- decisions/2026-07-27-source-prefix-is-run-distinct.md — `sourcePrefix` is a BARE token, no trailing slash (the engine composes `${sourcePrefix}/${msp.id}-integration` at mitosis.js:420; `REF_TOKEN_PATTERN` :2548 rejects `msp/`, which killed a dispatch in 12ms at stage `input`) AND it is RUN-DISTINCT, never the literal `msp` every run. 241 refs already live under `msp/`, and the engine REUSES an existing branch rather than failing — `worktree add ${wt} ${branch}` without `-b` (:1012-1013) — so a colliding decomposer slug silently builds on a months-old branch. `design-tokens` and `sticker-widget-kit` were live near-misses for A1/A4. Cluster A uses `msp-cluster-a`; each later cluster takes its own. Supersedes 2026-07-25-source-prefix-is-a-bare-token.md and only the literal token in the run contract
- decisions/2026-07-12-human-gated-merge-policy.md — autonomous is structurally blocked by the harness classifier (delegated agents can't self-merge); switched to mitosis "human-gated" mode + main-thread merge with per-session consent, layer-by-layer (still governs Option B builds)
- decisions/2026-07-10-client-implementation-stack.md — Riverpod 3.x + drift; ULID/updated_at/deleted_at/content-addressed media in v1
- decisions/2026-07-09-tech-stack.md — Flutter + SQLite both sides + custom REST sync + E2EE + Tailscale + Docker Compose
- decisions/2026-07-09-product-model.md — day/entry/memory model, 10 moods, streak, reminders, navigation
- decisions/2026-07-09-design-baseline.md — Field Notes prototype is the canonical visual baseline

## Threads
- journal-app-design, video-card-playback-controls — both done — closure summaries demoted verbatim to sessions/2026-07-28-07-prototype-design-alignment.md (cap enforcement); both thread files unchanged on disk
- post-ship-hardening — paused — PR #35 MERGED (5cb5bad) and hardware-confirmed: video plays, new voice plays repeatedly. 2 criteria remain: stop integration tests writing into the real container, and confirm an exported ZIP carries extensions.
- prototype-design-alignment — paused — **CLUSTER H CLOSED; THE SPEC HAS NO MSPs LEFT.** Every MSP is on main at `712c978`. Cluster H shipped as native stack 127 (#122 slice, #123-#126) merged top-down, the FOURTH clean use of the pattern. H1 was decomposed into FOUR stacked MSPs ordered by FONT RISK — harness plus text-free cross-hatch, then blooms, then sticker cards and nav icons, then the three text-bearing sticker buttons at the TOP — so the text-bearing family can be dropped without losing the harness. **The toolchain gate that blocked the last MSP is ANSWERED and CLOSED**: fontWeight DOES drive the wght axis on 3.44.8, proven by a w100-w900 sweep with per-family fvar plateaus plus an Ahem negative control. The suite went 918 -> 944 with 24 goldens where there was previously ZERO golden coverage; all four MSPs predicted their own totals exactly. **The perturbation receipt HOLDS** — a one-pixel shadow-offset change reds exactly `sticker_button_primary` at 1.21%/110px while its two siblings stay green, so the harness discriminates rather than merely running. Code review returned ZERO blocking findings. `.github/workflows/goldens.yml` is NEW and HAS NEVER EXECUTED — its first run is the next PR touching its `paths:` filter, and a mass red there must NOT be answered with `--update-goldens`. **OQ-3 and OQ-6 are the ONLY unmet completion criterion**; both are researched with verified-against-code recommendations in sessions/2026-08-02-02. Cluster H needs no visual pass — it touched zero files under `lib/`. Branch disposal owed for the Cluster G and H sets plus ledger-handoff-session-27 and -28

## State snapshot (2026-07-22)
- CAPTURE FLOWS COMPLETE + LOCAL RUN ACHIEVED — demoted verbatim to sessions/2026-07-24-09-video-card-playback-controls.md (cap enforcement). Load-bearing residue: always run via `flutter run -d macos`; screenshot through the `_flutter.screenshot` VM RPC (scratchpad/vm_screenshot.dart), `screencapture` is TCC-blocked; set LANG=en_US.UTF-8 for CocoaPods.
- NO ANDROID SDK on this machine — `flutter build apk` is impossible, and receipts CI never builds Android. File-content assertion is the only receipt for the new desugaring task; do NOT add an Android build to receipts.config.json.
- THIS PROJECT'S LEDGER IS v1-MARKDOWN ONLY. Two continuity systems are visible; only the file ledger under `.claude/ledger/` is populated. The `session-continuity` MCP store is SEPARATE and EMPTY here — its `thread_id` is a 26-char ULID while these threads are slug-named markdown, and there are ZERO non-markdown artifacts under `.claude/ledger/` (no index, no db, no json). That is why SessionStart reports "no resumable threads" while the prompt hook lists them. Use `/resume-project` to read and `/session-handoff` to write; invoking `lift-off`/`ledgerize` writes into an empty parallel store and will silently fail to move a thread. Demoted to make room: sessions/2026-07-26-03-video-card-playback-controls.md
- THE PROJECT IS NAMED "field-notes" (repo https://github.com/SatanshuMishra/field-notes, PRIVATE). "fireplace" is NOT the project name — it survives only as the local directory path and the .fireplace-worktrees root, both legacy and both load-bearing for hardcoded paths. Never call the project fireplace. The `origin` remote was repointed to the field-notes URL on 2026-07-20 (the old URL only worked via a 301 redirect); this supersedes the "repoint was denied" note in decisions/2026-07-10-mitosis-run-contract.md.
- WORKTREES ARE KEPT ON PURPOSE (decisions/2026-07-20-keep-stale-worktrees.md). Do not propose cleanup.

## Pointers
- docs/superpowers/specs/2026-07-10-field-notes-design.md — v1 design spec (§0 = implementation status + pre-vendored fonts)
- docs/design/prototype-analysis.md — full prototype extraction + 14 reconciliation points
- docs/specs/2026-07-26-prototype-design-alignment.md — CURRENT line of work: 39 MSPs aligning the app to the prototype, and the standing authority + citation source. §2.7 binds preserve items to MSPs; §7 warns the spec has been wrong about citation line numbers once — re-open every cited line. Execution slices are cut from it per cluster: docs/specs/2026-07-27-prototype-alignment-cluster-a.md (Cluster A, executed) and docs/specs/2026-07-27-prototype-alignment-cluster-b.md (Cluster B, fully executed — B1-B4 all merged) and docs/specs/2026-07-28-prototype-alignment-cluster-c.md (Cluster C, merged #71, NOT yet executed — it is the live working document for C1-C7). Clusters D-H still need their slices cut. NOTE the §7 warning is now understated: the spec family has been wrong about citation line numbers THREE times, the latest being the video-tile row, corrected to `:128-130` in the Cluster C slice
- .claude/ledger/threads/prototype-design-alignment.md — current thread; its Recent Sessions list is the session trail
- decisions 2026-07-22-capture-finalize-fix-strategy / 2026-07-22-save-hang-timeout-noop-root-cause / 2026-07-19-entry-cards-fix-and-batch-2, and plans/2026-07-24-video-controls-overlay-plan.md — demoted to sessions/2026-07-27-01-prototype-design-alignment.md; the docs/superpowers/specs/2026-07-25-video-poster-first-and-feed-virtualization.md pointer demoted to sessions/2026-07-27-02-prototype-design-alignment.md, now carried by decisions/2026-07-26-msp4-today-virtualization-not-authorized.md (cap enforcement; all files remain on disk unchanged); decisions 2026-07-24-share-plus-13-blocked-by-file-picker / 2026-07-24-macos-videorotationangle-crash demoted to sessions/2026-07-27-04-prototype-design-alignment.md; the engine-era pointers (2026-07-19-next-round plan, sessions 2026-07-19-02 / 2026-07-16-02 / 2026-07-11-03) are listed in sessions/2026-07-25-01-video-card-playback-controls.md and demoted to sessions/2026-07-27-06-prototype-design-alignment.md; decisions 2026-07-24-combined-camera-deps-pr / 2026-07-24-camera-preview-lifecycle-root-causes / 2026-07-21-capture-flow-root-cause-and-fix / 2026-07-21-vm-rpc-screenshot-for-visual-verification demoted to sessions/2026-07-27-08-prototype-design-alignment.md (the last two have their load-bearing residue in the State snapshot above); decisions/2026-07-26-day-detail-shrinkwrap-option-b.md demoted to sessions/2026-07-28-02-prototype-design-alignment.md; decisions/2026-07-27-pr-title-lint-fix-belongs-at-the-title.md demoted 2026-07-28 to make room for the Cluster C mitosis decision — its defect is closed (PR #58 landed the missing `edited` trigger, so a corrected title now clears the red) and the residue is that the lint is right, the title is wrong, and loosening the type alternation is REJECTED — every file remains on disk unchanged; three mitosis-operational decision index lines (2026-07-28-parked-ship-resumes-by-re-execution, 2026-07-28-mitosis-requires-serena-activation, 2026-07-27-mitosis-resume-contract) demoted 2026-07-29 to sessions/2026-07-29-01-prototype-design-alignment.md, spent now that mitosis is excluded for the whole remaining spec — all three files remain on disk unchanged
- integration_test/capture_ui_flow_test.dart — real-UI -d macos capture receipt (note/voice/video)
