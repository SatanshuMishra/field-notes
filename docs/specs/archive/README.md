# Archived specs — superseded 2026-09-20

Both documents below are ARCHIVED and NON-BINDING. They were superseded by a
clean-slate re-specification requested by the user on 2026-09-20.

They are kept for history only. Nothing in them constrains the new spec: not a
decision, not a resolution, not a rejected alternative, not a phase ladder.
Do NOT read them when designing or implementing the replacement.

- `2026-08-03-rich-editor-and-inline-photos.md` — combined editor + inline photo spec.
  Written, hardened once, patched six times, then failed a convergence pass 3-0
  with 24 defects. Never implemented; zero product code exists.
- `2026-08-02-inline-photo-notes.md` — the P0-P5 inline photo ladder it amended.
  Never critic-hardened.

Why they were abandoned rather than repaired: the accumulated patches contradicted
each other faster than they converged, and the new requirements (true free
positioning, responsive reflow across desktop/Android/iOS) are broader than the
model those documents were built on.
