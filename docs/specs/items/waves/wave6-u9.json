[
 {
  "name": "u9",
  "files": [
   "lib/features/notes/render/note_photo_plan.dart",
   "lib/features/notes/render/note_photo_block.dart",
   "lib/features/entry_cards/notes/note_document.dart",
   "lib/features/notes/notes.dart",
   "lib/features/notes/render/photo_wrap_block.dart",
   "lib/features/notes/render/float_split_cache.dart",
   "lib/features/notes/render/note_render_budget.dart",
   "test/features/notes/render/note_photo_plan_test.dart",
   "test/features/entry_cards/notes/note_document_test.dart",
   "test/features/notes/render/float_plan_table_test.dart",
   "test/features/notes/render/float_split_cache_test.dart",
   "test/features/notes/render/photo_wrap_block_test.dart",
   "test/design/goldens/note_float_golden_test.dart"
  ],
  "after": [],
  "complexity": "complex",
  "type": "feature",
  "task": "READ FIRST: `docs/specs/2026-09-20-note-editor-and-scrapbook-photos.md` - section 3 (Architecture) and section 5's `u9` block are binding.\nResearch backing it, read if you need the derivation: docs/specs/research/2026-09-20-design-panel.md\nEMBARGO: do NOT read docs/specs/archive/** - superseded and non-binding.\n\nPROJECT RULES (non-negotiable, this repo):\n- NEVER write code comments, docstrings or JSDoc. The code is the only source of truth.\n- NEVER add AI co-author attribution to commits.\n- NO new package dependency. NO database migration: schemaVersion stays 1 and onUpgrade must keep throwing.\n- Verification is `export PATH=\"/opt/homebrew/bin:$PATH\" && flutter pub get && dart run build_runner build --delete-conflicting-outputs && flutter analyze && flutter test`, run in the FOREGROUND and READ. CI is not evidence - no GitHub check runs a Dart test here.\n- NEVER run `flutter test integration_test/` as a directory: capture_save_persist_test.dart writes into the real journal container. Name a single file.\n- Measure the test baseline on your own branch before and after. Never inherit a count.\n- An existing test that pins behaviour you are MANDATED to change may be retargeted to pin the SAME behaviour through the new surface. It may not be deleted or weakened.\n- You cannot run the app: `flutter run -d macos` needs a TTY. Do not launch it. Visual confirmation is a human step.\n\n=== YOUR UNIT: u9 - The float ===\n\nplanFloat(measure, em, sizeEm, side, aspect) implementing one clamp and one comparison. em = textScaler.scale(fontSize); measure = min(maxWidth, 35 em); gutter = 1 em; cap = measure - gutter - 19.4 em; photoW = min(sizeEm * em, cap) so the photo SHRINKS BEFORE IT DEMOTES; FLOAT iff size != full and the next block is a paragraph and photoW >= 8.5 em; else stacked. photoH = min(photoW / aspect, 1.6 * photoW) so a portrait photo cannot become a wall.\nSizes are in em, not fractions of the measure: with a fraction, a narrowing window makes the SMALL size demote before the medium one, because the fraction term drops below the minimum-photo gate while the cap has not yet bitten.\nPhotoWrapBlock performs the split inside one LayoutBuilder: a TextPainter over the paragraph span at the band width, computeLineMetrics, find the last line fitting beside the photo, getLineBoundary to snap the cut to a line START (never a word boundary - a mid-line cut is the only way this looks broken), then sliceInlineSpan and emit a Row of photo plus head with the tail below.\nFLOAT SCOPE RULE, the single most important constraint: a float wraps the NEXT PARAGRAPH BLOCK ONLY. If the next block is a heading, a list, another photo or the end of the note, the photo stacks. Two floats therefore cannot interact, clear is structural rather than implemented, and the TextPainter measures one paragraph rather than the note.\nONE FALLBACK: StackedPhoto is the single return value for every failure - column too narrow, size full, next block not a paragraph, missing or unreadable dimensions, corrupt blob, unparseable token, aspect past the clamp, or the render budget disabling floats. One path, exercised by every phone on every note forever, so it is proven in production long before a slow desktop needs it.\nTilt, paper frame and shadow are paint INSIDE the reserved axis-aligned box, with the plan reserving w + h*sin|theta| so the wrap contour never becomes non-rectangular.\nSTRESS FIX (performance): cache the split offset per (span identity, text scaler, band bucket) with one line of hysteresis, or a continuous desktop window resize re-splits and re-shapes the paragraph every frame.\nSTRESS FIX: either make the block width at the demote boundary continuous with the float width it replaces, or stop claiming there is no jump and state the step plainly in the decision table.\nShips with a decision table across widths and text scales, plus goldens.\n\nSUPPORTING CONTEXT - read these, do NOT edit them:\n  - docs/specs/2026-08-02-prototype-alignment-cluster-h.md\n  - docs/specs/research/2026-09-20-design-panel.md\n  - docs/specs/research/2026-09-20-terrain-survey.md\n  - lib/design/tokens/typography.dart\n  - lib/design/widgets/sticker_card.dart\n  - lib/features/entry_cards/cards/note_body.dart\n  - lib/features/entry_cards/entry_card.dart\n  - lib/features/entry_cards/media/media_image.dart\n  - lib/features/notes/model/inline_slice.dart\n  - lib/features/notes/model/note_block.dart\n  - test/design/goldens/golden_harness.dart\n  - test/design/goldens/sticker_card_golden_test.dart\n  - test/design/widgets/widget_harness.dart\n  - test/features/entry_cards/support/entry_cards_harness.dart\n\n=== BASE STATE, read before you touch anything. THIS SECTION OVERRIDES THE FILE PATHS ABOVE. ===\nUnits u1 through u8 have ALREADY MERGED. The original plan for this unit was written before u8\nexisted and would have CREATED a second planFloat, a second NoteDocument and a second stacked\nrenderer. Those already exist. Creating another copy of any of them is a defect: two\nimplementations of the core layout rule will drift apart.\n\nWHAT EXISTS - USE IT, EXTEND IT, NEVER DUPLICATE IT:\n\n  THE PLAN (u8) - lib/features/notes/render/note_photo_plan.dart\n    PhotoPlan planFloat({required double measure, required double em, side, size, aspect})\n    final class PhotoPlan { side, size, measure, width, height, isStacked, measureCanFloat }\n    bool canFloatAt({required double measure, required double em})\n    Constants already match the spec: photoGutterEm 1, photoBandResidualEm 19.4,\n    photoMinFloatEm 8.5, photoHeightClamp 1.6.\n\n  THE PHOTO RENDERER (u8) - lib/features/notes/render/note_photo_block.dart\n    Calls planFloat, then ALWAYS returns a centred Column. It never floats. That is the gap you\n    close. Its stacked path, unavailable-photo placeholder and caption are correct - keep them.\n\n  THE DOCUMENT (u4) - lib/features/entry_cards/notes/note_document.dart   NoteDocument\n  THE SPLIT HELPER (u4) - lib/features/entry_cards/notes/inline_span_slice.dart   sliceInlineSpan\n    Already exists and is tested. Nothing calls it yet. You are its first consumer.\n\n  THE PARSER (u4) - lib/domain/notes/  PhotoBlock and ParagraphBlock already exist.\n\nWHAT DOES NOT EXIST YET - this is your actual work:\n  1. The paragraph split (photo_wrap_block.dart). Nothing in the codebase calls\n     computeLineMetrics, getLineBoundary or wraps text beside a photo.\n  2. The float scope rule in NoteDocument: pair a PhotoBlock with the NEXT ParagraphBlock only.\n  3. Honouring plan.isStacked in note_photo_block.dart instead of always centring.\n  4. The split cache with hysteresis, and the render budget.\n  5. The decision table and the goldens.\n\nDO NOT touch .github/workflows/. The goldens workflow runs `flutter test --tags golden` and picks\nup a golden-tagged test automatically.\n\nTHE EDITOR IS OUT OF SCOPE. R1: text wraps around a photo in the READ VIEW ONLY. Nothing under\nlib/features/capture/ changes in this unit.\n",
  "acceptance": "With `flutter test` green, a Photo block whose side is left or right and whose next block is a Paragraph renders as a Row of the photo beside a head slice cut at a line boundary with the tail below exactly when `planFloat` returns a non-stacked plan, every other input (size full, next block not a paragraph, `photoW < 8.5 em`, missing aspect, corrupt blob, `floatEnabled == false`) returns `StackedPhoto`, and both the ~360-row `planFloat` decision table and the twelve width-by-text-scale goldens pass.",
  "file_notes": {
   "lib/features/notes/render/note_photo_plan.dart": "EXISTS. planFloat / PhotoPlan / canFloatAt live here already. Extend PhotoPlan only if the split needs a value it lacks (e.g. the band width). Do NOT create a second planning function anywhere.",
   "lib/features/notes/render/note_photo_block.dart": "EXISTS. Computes a PhotoPlan and then ALWAYS renders a centred Column, ignoring plan.isStacked. The stacked path is correct and stays; this file must hand a floating plan to the wrap instead of centring it.",
   "lib/features/entry_cards/notes/note_document.dart": "EXISTS. The float scope rule lives here: a PhotoBlock may float only beside the IMMEDIATELY FOLLOWING ParagraphBlock, so the document must pair them. A single block renderer cannot see its neighbour.",
   "lib/features/notes/notes.dart": "EXISTS. Barrel - export the new render files.",
   "lib/features/notes/render/photo_wrap_block.dart": "NEW. The paragraph split: TextPainter at the band width, computeLineMetrics, last line fitting beside the photo, getLineBoundary to snap to a line START, then the existing sliceInlineSpan. Emit photo + head in a Row, tail below.",
   "lib/features/notes/render/float_split_cache.dart": "NEW. Cache the split offset per (span identity, text scaler, band bucket) with one line of hysteresis, so a continuous window resize does not re-split every frame.",
   "lib/features/notes/render/note_render_budget.dart": "NEW. The render budget that can disable floats - one of the conditions that must route to the stacked fallback.",
   "test/features/notes/render/note_photo_plan_test.dart": "EXISTS. Existing planFloat tests. Extend if you extend PhotoPlan; do not weaken.",
   "test/features/entry_cards/notes/note_document_test.dart": "EXISTS. Existing NoteDocument tests. Add the pairing behaviour; do not weaken what is pinned.",
   "test/features/notes/render/float_plan_table_test.dart": "NEW. The decision table across widths and text scales, asserted through planFloat.",
   "test/features/notes/render/float_split_cache_test.dart": "NEW. Cache hit, miss and hysteresis.",
   "test/features/notes/render/photo_wrap_block_test.dart": "NEW. No glyph box of head or tail overlaps the photo rect; the cut is always a line start; selection across the head/tail boundary copies the paragraph byte-exact.",
   "test/design/goldens/note_float_golden_test.dart": "NEW. Tag it `golden` (see dart_test.yaml) and use test/design/goldens/golden_harness.dart. CI's goldens workflow selects by that tag, so no workflow change is needed. Generated images land in test/design/goldens/images/."
  }
 }
]